package co.com.farmatodo.api.dto.client;

import io.swagger.v3.oas.annotations.media.Schema;

@Schema(
        name = "ClientDTO",
        description = "DTO for client information response"
)
public class ClientDTO {
    // Add fields and annotations if needed
}
